---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: restartrules.karo.jeeatwork.com
spec:
  group: karo.jeeatwork.com
  names:
    kind: RestartRule
    listKind: RestartRuleList
    plural: restartrules
    singular: restartrule
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RestartRule is the Schema for the restartrules API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RestartRule
            properties:
              changes:
                description: Changes defines the resources whose changes trigger restarts
                items:
                  description: ChangeSpec defines a resource whose changes trigger
                    restarts
                  properties:
                    changeType:
                      description: |-
                        ChangeType specifies which types of changes should trigger restarts
                        If not specified, only "Update" changes will trigger restarts
                      items:
                        type: string
                      type: array
                    isRegex:
                      description: |-
                        IsRegex indicates if Name should be treated as a regular expression pattern
                        If false (default), Name is treated as a literal string match
                      type: boolean
                    kind:
                      description: Kind specifies the kind of resource to watch for
                        changes
                      enum:
                      - ConfigMap
                      - Secret
                      type: string
                    name:
                      description: Name specifies the name of the resource to watch
                      type: string
                    selector:
                      description: Selector allows watching resources by label selector
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                  - kind
                  type: object
                minItems: 1
                type: array
              delayRestart:
                description: |-
                  DelayRestart specifies the number of seconds to wait before triggering a restart
                  after a change is detected. If not specified, restarts are triggered immediately.
                  When multiple RestartRules with different delays target the same workload, the
                  highest delay value is used to ensure all rules' delay requirements are met.
                format: int32
                maximum: 3600
                minimum: 0
                type: integer
              targets:
                description: Targets defines the resources to restart when changes
                  are detected
                items:
                  description: TargetSpec defines a resource to restart when changes
                    are detected
                  properties:
                    kind:
                      description: Kind specifies the kind of resource to restart
                      enum:
                      - Deployment
                      - StatefulSet
                      type: string
                    name:
                      description: Name specifies the name of the resource to restart
                      type: string
                    namespace:
                      description: |-
                        Namespace specifies the namespace of the target resources
                        If not specified, the controller will use the namespace of the RestartRule resource
                      type: string
                    selector:
                      description: Selector allows targeting resources by label selector
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                  - kind
                  type: object
                minItems: 1
                type: array
            required:
            - changes
            - targets
            type: object
          status:
            description: status defines the observed state of RestartRule
            properties:
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastProcessedAt:
                description: LastProcessedAt indicates when the rule was last evaluated
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the RestartRule
                  lifecycle
                enum:
                - Pending
                - Active
                - Invalid
                - Failed
                type: string
              restartHistory:
                description: RestartHistory tracks recent restart actions triggered
                  by this rule
                items:
                  description: RestartEvent records a restart action triggered by
                    a RestartRule
                  properties:
                    message:
                      description: Message provides additional details about the restart
                      type: string
                    status:
                      description: Status indicates whether the restart was successful
                      enum:
                      - Success
                      - Failed
                      type: string
                    target:
                      description: Target identifies the workload that was restarted
                      properties:
                        kind:
                          description: Kind of the workload (Deployment, StatefulSet,
                            etc.)
                          type: string
                        name:
                          description: Name of the workload
                          type: string
                        namespace:
                          description: Namespace of the workload
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                    timestamp:
                      description: Timestamp when the restart was triggered
                      format: date-time
                      type: string
                    triggerResource:
                      description: TriggerResource identifies the resource change
                        that triggered the restart
                      properties:
                        kind:
                          description: Kind of the resource (ConfigMap, Secret, etc.)
                          type: string
                        name:
                          description: Name of the resource
                          type: string
                        namespace:
                          description: Namespace of the resource
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                  required:
                  - status
                  - target
                  - timestamp
                  - triggerResource
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
